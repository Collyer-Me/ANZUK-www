{
  "name": "anzuk-marketing-hub",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @anzuk/web",
    "dev:web": "npm run dev -w @anzuk/web",
    "build": "npm run build -w @anzuk/web",
    "build:web": "npm run build -w @anzuk/web",
    "check:web": "npm run check -w @anzuk/web",
    "seed:storyblok": "tsx scripts/seed-storyblok.ts",
    "configure:storyblok-deploy": "tsx scripts/configure-storyblok-deploy.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
