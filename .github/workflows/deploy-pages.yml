name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  repository_dispatch:
    types: [storyblok-publish]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Deploy trigger reason'
        required: false
        default: 'manual'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Astro site
        run: npm run build -w @anzuk/web
        env:
          SITE_URL: ${{ secrets.SITE_URL }}
          BASE_PATH: ${{ secrets.BASE_PATH }}
          STORYBLOK_DELIVERY_API_TOKEN: ${{ secrets.STORYBLOK_DELIVERY_API_TOKEN }}
          STORYBLOK_REGION: ${{ secrets.STORYBLOK_REGION || 'eu' }}
          ASTRO_OUTPUT: static
          CI: true
          PUBLIC_KETCH_ORG: ${{ secrets.PUBLIC_KETCH_ORG }}
          PUBLIC_KETCH_PROPERTY: ${{ secrets.PUBLIC_KETCH_PROPERTY }}
          PUBLIC_RUDDERSTACK_WRITE_KEY: ${{ secrets.PUBLIC_RUDDERSTACK_WRITE_KEY }}
          PUBLIC_RUDDERSTACK_DATA_PLANE_URL: ${{ secrets.PUBLIC_RUDDERSTACK_DATA_PLANE_URL }}
          PUBLIC_GTM_ID: ${{ secrets.PUBLIC_GTM_ID }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
