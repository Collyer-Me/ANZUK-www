---
import Container from '../ui/Container.astro';
import Section from '../ui/Section.astro';
import { demoStats } from '../../data/demo-content';

const numberFormat = new Intl.NumberFormat('en-AU');
---

<Section variant="darkPurple" padding="compact" aria-labelledby="stats-heading">
  <Container>
    <h2 id="stats-heading" class="sr-only">ANZUK by the numbers</h2>
    <div class="grid gap-10 text-center sm:grid-cols-2 lg:grid-cols-4">
      {
        demoStats.map((stat, index) => (
          <div data-reveal="up" data-reveal-delay={String(index * 100)}>
            <p class="text-5xl font-bold text-white">
              <span data-counter={String(stat.value)} data-counter-suffix={stat.suffix}>
                {numberFormat.format(stat.value)}{stat.suffix}
              </span>
            </p>
            <p class="mt-2 text-sm text-anzuk-light-blue">{stat.label}</p>
          </div>
        ))
      }
    </div>
    <p class="mt-10 text-center text-xs text-anzuk-light-gray/70">Illustrative figures for demo purposes.</p>
  </Container>
</Section>
